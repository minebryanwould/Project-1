local player = game.Players.LocalPlayer
local defaultSpeed = 20
local boostedSpeed = 40
local jumpResetTime = 0.2

local function setupCharacter(character)
    local humanoid = character:WaitForChild("Humanoid")
    humanoid.WalkSpeed = defaultSpeed

    local jumpCounter = 0
    local lastJumpTime = 0

    local function resetJumpCounter()
        jumpCounter = 0
    end

    humanoid:GetPropertyChangedSignal("Jump"):Connect(function()
        if humanoid.Jump then
            local currentTime = tick()
            if currentTime - lastJumpTime > jumpResetTime then
                resetJumpCounter()
            end
            
            jumpCounter += 1
            lastJumpTime = currentTime
            
            if jumpCounter == 3 then
                resetJumpCounter()
                if humanoid.WalkSpeed == defaultSpeed then
                    humanoid.WalkSpeed = boostedSpeed
                else
                    humanoid.WalkSpeed = defaultSpeed
                end
            end
        end
    end)

    humanoid:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
        if humanoid.WalkSpeed ~= defaultSpeed and humanoid.WalkSpeed ~= boostedSpeed then
            humanoid.WalkSpeed = humanoid.WalkSpeed == boostedSpeed and boostedSpeed or defaultSpeed
        end
    end)
end

if player.Character then
    setupCharacter(player.Character)
end

player.CharacterAdded:Connect(setupCharacter)
